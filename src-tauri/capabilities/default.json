{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay windows",
  "windows": ["draw-*", "boxes-*", "settings"],
  "permissions": [
    "core:default"
  ]
}
